Provisioning scripts to configure [Vagrant](https://www.vagrantup.com/)
virtual machines.

## Usage

Run `vagrant status` to see the available machines. To start a machine, run:

```bash
vagrant up [name]
vagrant ssh [name]
```
